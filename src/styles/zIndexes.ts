export const zIndexes = {
  dialog: 32,
  dropdownMenu: 36,
  header: 30,
  hoverCard: 28,
  dialogHoverCard: 34,
  /** Above sticky header CSS z-index (50) so modal menus trap focus. */
  mobileNavOverlay: 60,
}
